═══════════════════════════════════════════════════════
TURTLE DRAW - EXECUTION INSTRUCTIONS
═══════════════════════════════════════════════════════

✅ GUARANTEED TO WORK - Follow These Exact Steps:

═══════════════════════════════════════════════════════
TERMINAL 1 - START TURTLESIM
═══════════════════════════════════════════════════════

Copy and paste these commands ONE BY ONE:

1. source ~/.zshrc
2. micromamba activate ros_env
3. ros2 run turtlesim turtlesim_node

→ A window with a turtle will appear and stay open

═══════════════════════════════════════════════════════
TERMINAL 2 - RUN TURTLE DRAWER
═══════════════════════════════════════════════════════

Copy and paste these commands ONE BY ONE:

1. source ~/.zshrc

2. micromamba activate ros_env

3. cd ~/Desktop/ponderada_ros/turtle_draw_ws

4. source install/setup.bash

5. ros2 run turtle_draw_pkg turtle_drawer

→ The turtle will start drawing! Watch turtlesim window

═══════════════════════════════════════════════════════
IF YOU SEE "Package not found"
═══════════════════════════════════════════════════════

You didn't run all the steps above. Specifically:
- Did you run: source ~/.zshrc
- Did you run: micromamba activate ros_env
- Did you run: source install/setup.bash

All three are REQUIRED.

═══════════════════════════════════════════════════════
FOR TESTING (WITHOUT RUNNING TURTLESIM)
═══════════════════════════════════════════════════════

This will verify the vision pipeline works:

1. source ~/.zshrc
2. micromamba activate ros_env
3. python3 ~/Desktop/ponderada_ros/test_vision_pipeline.py

Expected: ✅ 178 contours found

═══════════════════════════════════════════════════════
QUICK VERSION (Copy whole block and paste)
═══════════════════════════════════════════════════════

Terminal 1:
source ~/.zshrc && micromamba activate ros_env && ros2 run turtlesim turtlesim_node

Terminal 2:
source ~/.zshrc && micromamba activate ros_env && cd ~/Desktop/ponderada_ros/turtle_draw_ws && source install/setup.bash && ros2 run turtle_draw_pkg turtle_drawer

═══════════════════════════════════════════════════════
